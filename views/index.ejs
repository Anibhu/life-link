<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LifeLink</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="logo">
            <img src="images/hospital.png" alt="Logo">
            <h1>LIFE-LINK</h1>
        </div>
        <ul class="nav-links">
            <li><a href="#home" class="active">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
        <div class="auth-buttons">
            <% if (isLoggedIn) { %>
                <span class="username">
                    <a href="/dashboard" style="text-decoration: none; color: inherit;">
                        <%= username %>!
                    </a>
                </span>
                <form action="/logout" method="POST" style="display: inline;">
                    <button type="submit" style="border: none; background: none; cursor: pointer;">
                        🔒
                    </button>
                </form>         
            <% } else { %>
                <button class="btn-login-btn" onclick="window.location.href='/login'">Login</button>
                <button class="btn-signup-btn" onclick="window.location.href='/registration'">Sign Up</button>
            <% } %>
        </div>
        <div class="menu-toggle" id="mobile-menu">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </div>
    </nav>

    <section id="home" class="hero">
        <div class="hero-content">
            <h1>Save Lives, Donate Today</h1>
            <div class="banner">
                <h2>Give the Gift of Life - Donate Blood & Organs</h2>
            </div>
            <div class="cta-buttons">
                <button class="btn-register-btn" onclick="window.location.href='/donor_registration'">Register as a Donor</button>
                <button class="btn-request-btn" onclick="window.location.href='/organs'">Looking for a Donor</button>
            </div>
        </div>
    </section>

    <section id="about" class="about">
        <h2>About Us</h2>
        <div class="about-content">
            <img src="images/Blood-Donation-2.jpg" alt="Blood Donation">
            <p>
                LifeLink is a platform dedicated to connecting patients in need of organ transplants and blood donations with compatible donors. Our mission is to bridge the gap between donors and recipients by providing a seamless and efficient system for registration, searching, and matching. Users can create accounts, register as donors, and request organ transplants while hospitals can update real-time availability. With an easy-to-use dashboard, patients can track their requests, view donor details, and stay informed about their health needs. LifeLink ensures secure authentication, user-friendly navigation, and a reliable database, making the process of saving lives more accessible and efficient.
            </p>
        </div>
    </section>

    <section id="contact" class="contact">
        <div class="contact-content">
            <h2>Contact Us</h2>
            <p>Have questions or want to get involved? We'd love to hear from you!</p>
            <div class="contact-form">
                <form id="contactForm">
                    <div class="form-group">
                        <label for="name">Name:</label>
                        <input type="text" id="name" name="name" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email:</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="message">Message:</label>
                        <textarea id="message" name="message" rows="5" required></textarea>
                    </div>
                    <button type="submit" class="btn-submit">Send Message</button>
                </form>
                <div id="form-response"></div>
            </div>
        </div>
    </section>

    <script>
        const mobileMenu = document.getElementById('mobile-menu');
        const navLinks = document.querySelector('.nav-links');

        mobileMenu.addEventListener('click', () => {
            navLinks.classList.toggle('active');
        });

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

        document.getElementById('contactForm').addEventListener('submit', function(event) {
            event.preventDefault();
            const formResponse = document.getElementById('form-response');
            formResponse.innerHTML = "Thank you for your message. We will get back to you soon!";
            const formData = new FormData(event.target);
            // AJAX call to submit form data
        });
    </script>
</body>
</html>

<style>
body {
    font-family: 'Roboto', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    line-height: 1.6;
    background-color: #F9FAFB;
    color: #1F2937;
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    background: linear-gradient(to right, #1E3A8A, #2563EB);
    color: white;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.logo {
    display: flex;
    align-items: center;
    gap: 10px;
}

.logo img {
    width: 40px;
    height: 40px;
}

.logo h1 {
    font-size: 1.5rem;
    font-weight: bold;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

.nav-links li {
    display: inline;
}

.nav-links a {
    color: white;
    text-decoration: none;
    font-weight: 500;
    padding: 8px 16px;
    transition: 0.3s;
    border-radius: 5px;
}

.nav-links a:hover,
.nav-links a.active {
    background-color: rgba(255, 255, 255, 0.2);
}

.auth-buttons {
    display: flex;
    align-items: center;
    gap: 10px;
}

.btn-login-btn, .btn-signup-btn {
    background-color: white;
    color: #1E3A8A;
    padding: 8px 16px;
    border: none;
    border-radius: 5px;
    font-weight: 500;
    cursor: pointer;
    transition: 0.3s;
}

.btn-login-btn:hover, .btn-signup-btn:hover {
    background-color: #F59E0B;
    color: white;
}

.menu-toggle {
    display: none;
    flex-direction: column;
    cursor: pointer;
}

.bar {
    height: 4px;
    width: 25px;
    background-color: white;
    margin: 4px 0;
    transition: 0.3s;
}

.hero, .about, .contact {
    text-align: center;
    padding: 4rem 2rem;
}

.hero {
    background-color: #1E3A8A;
    color: white;
}

.hero h1, .about h2, .contact h2 {
    font-size: 2.5rem;
    font-weight: bold;
    margin-bottom: 1rem;
    color: #2563EB;
}

.about, .contact {
        background: linear-gradient(to right, #1E3A8A, #2563EB);
        text-align: center;
        padding: 4rem 2rem;
        color: white;
    }

    .about img { 
        width: 50%; max-width: 400px; border-radius: 10px; 
    }

.btn-register-btn, .btn-request-btn {
    padding: 1rem 2rem;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-weight: 500;
    color: white;
    margin: 0.5rem;
}

.btn-register-btn { background-color: #10B981; }
.btn-request-btn { background-color: #F59E0B; }
.btn-register-btn:hover, .btn-request-btn:hover { opacity: 0.9; }

.contact-content {
        max-width: 600px;
        margin: 0 auto;
    }

    .contact h2, .about h2 {
        font-size: 2rem;
        font-weight: bold;
        margin-bottom: 1rem;
        color: white;
    }

    .contact p, .about p {
        font-size: 1rem;
        color: #E0E7FF;
        margin-bottom: 2rem;
    }

    .contact-form {
        background: white;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        margin: 0 auto;
    }

    .form-group {
        margin-bottom: 1.5rem;
        text-align: left;
    }

    .form-group label {
        display: block;
        font-weight: 500;
        margin-bottom: 0.5rem;
        color: #1E3A8A;
    }

    .form-group input,
    .form-group textarea {
        width: 100%;
        padding: 10px;
        border: 1px solid #D1D5DB;
        border-radius: 5px;
        font-size: 1rem;
        transition: border-color 0.3s;
    }

    .form-group input:focus,
    .form-group textarea:focus {
        border-color: #2563EB;
        outline: none;
        box-shadow: 0 0 5px rgba(37, 99, 235, 0.3);
    }

    .btn-submit {
        background-color: #2563EB;
        color: white;
        padding: 12px 20px;
        border: none;
        border-radius: 5px;
        font-size: 1rem;
        font-weight: 500;
        cursor: pointer;
        transition: background 0.3s;
        width: 100%;
    }

    .btn-submit:hover {
        background-color: #1E40AF;
    }

    #form-response {
        margin-top: 1rem;
        font-weight: 500;
        color: #16A34A;
    }
</style>